{
  "name": "vesta",
  "version": "0.5.1",
  "private": true,
  "description": "VESTA — Expanding Settlements Through Accord",
  "type": "module",
  "main": "src/vesta.ts",
  "scripts": {
    "check": "tsc --noEmit",
    "compile": "tsc -p tsconfig.web.json && node scripts/strip-exports.mjs",
    "pretest": "npm run compile",
    "test": "node --test",
    "dev": "npm run compile && npx serve web",
    "bake": "node web/bake.mjs"
  },
  "dependencies": {
    "urd": "github:fchurca/urd#semver:^0"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^6"
  }
}
